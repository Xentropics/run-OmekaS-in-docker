version: "3"
services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_DATABASE: ${OMEKA_DB}
      MYSQL_USER: ${OMEKA_DB_USER}
      MYSQL_PASSWORD: ${OMEKA_DB_PASSWORD}
      MYSQL_RANDOM_ROOT_PASSWORD: "1"
    volumes:
      - db:/var/lib/mysql
  omeka-s:
    image: xentropics/omeka-s:${tag}
    user: "1978:1978"
    links:
      - db:db
    restart: always
    depends_on:
      - db
    volumes:
      - omeka:/var/www/html/files
    ports:
      - 9001:80
volumes:
  db:
  omeka:
